#include "main.h"
#include <inttypes.h>
/**
 * int _abs(int): computes the absolute value of an integer
 * @n: integer passed as argument
 */

int _abs(int n)
{
	if (n < 0)
	{
		
		return (-n);
	}
	return (n);
}
